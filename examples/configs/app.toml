# Application settings in TOML format
app_name = "proteus-demo"
version = "1.0.0"
debug = false

[server]
host = "0.0.0.0"
port = 8080

[database]
host = "localhost"
port = 5432
name = "mydb"
user = "admin"
password = "secret"

[logging]
level = "INFO"
file = "app.log"
